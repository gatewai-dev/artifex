---
name: node-catalog
description: Authoritative catalog of all supported workflow canvas nodes, with links to their respective skills.
metadata:
  triggers: list nodes, supported node types, node catalog
---

# Node Catalog

Authoritative capabilities of every registered node. Input/output handles and dynamic (variable) inputs/outputs are read directly from each node manifest at build time — the same data `artifex nodes --json` reports. Featured nodes (0).


